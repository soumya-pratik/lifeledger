export type IncomeEntry = {
  id: string;
  ledgerId: string;
  month: string;
  amountMinor: number;
  currency: string;
  note: string;
  createdBy: string;
  createdAt: string;
  updatedAt: string;
};
